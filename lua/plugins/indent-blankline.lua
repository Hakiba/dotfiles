return {
  {
    "lukas-reineke/indent-blankline.nvim",
    opts = {
      indent = {
        highlight = {},
      },
    },
    config = function(_, opts)
      -- Initial check to set the correct state based on current wrap setting
    end,
  },
}
